model Account {
  id        String   @id @default(uuid()) // Using uuid() for unique indentification
  email     String   @unique // Create unique email for user authentication
  password  String
  name      String   // Users full name
  createdAt DateTime @default(now()) // Date for account creation
}
