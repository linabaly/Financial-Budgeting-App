model Goal {
    id              String          @id @default(uuid())
    account         Account
    name            String
    targetAmount    Decimal         @default(0)
    currentSaved    Decimal         @default(now())
    deadline        DateTime        @default(now())
    createdAt       DateTime        @default(now())
}
